# Web Card

### Main tools used to build this project:

1. Next.js
2. React
3. Typescript

This SPA is responsive to smaller screens but shows one time warning that it's recommended to use larger sceen.

Theme changing is handled by having special buttons on screen. Skills window is draggable, but dragging doesn't not work on mobile.
